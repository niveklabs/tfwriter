# rancher2_catalog_v2

[back](../rancher2.md)

### Index

- [Example Usage](#example-usage)
- [Variables](#variables)
- [Resource](#resource)
- [Outputs](#outputs)

### Terraform

```terraform
terraform {
  required_providers {
    rancher2 = ">= 1.13.0"
  }
}
```

[top](#index)

### Example Usage

```terraform
module "rancher2_catalog_v2" {
  source = "./modules/rancher2/r/rancher2_catalog_v2"

  # annotations - (optional) is a type of map of string
  annotations = {}
  # ca_bundle - (optional) is a type of string
  ca_bundle = null
  # cluster_id - (required) is a type of string
  cluster_id = null
  # enabled - (optional) is a type of bool
  enabled = null
  # git_branch - (optional) is a type of string
  git_branch = null
  # git_repo - (optional) is a type of string
  git_repo = null
  # insecure - (optional) is a type of bool
  insecure = null
  # labels - (optional) is a type of map of string
  labels = {}
  # name - (required) is a type of string
  name = null
  # secret_name - (optional) is a type of string
  secret_name = null
  # secret_namespace - (optional) is a type of string
  secret_namespace = null
  # service_account - (optional) is a type of string
  service_account = null
  # service_account_namespace - (optional) is a type of string
  service_account_namespace = null
  # url - (optional) is a type of string
  url = null

  timeouts = [{
    create = null
    delete = null
    update = null
  }]
}
```

[top](#index)

### Variables

```terraform
variable "annotations" {
  description = "(optional) - Annotations of the resource"
  type        = map(string)
  default     = null
}

variable "ca_bundle" {
  description = "(optional) - PEM encoded CA bundle which will be used to validate the repo's certificate"
  type        = string
  default     = null
}

variable "cluster_id" {
  description = "(required) - K8s cluster ID"
  type        = string
}

variable "enabled" {
  description = "(optional) - If disabled the repo clone will not be updated or allowed to be installed from"
  type        = bool
  default     = null
}

variable "git_branch" {
  description = "(optional) - Git Repository branch containing Helm chart definitions"
  type        = string
  default     = null
}

variable "git_repo" {
  description = "(optional) - Git Repository containing Helm chart definitions"
  type        = string
  default     = null
}

variable "insecure" {
  description = "(optional) - Use insecure HTTPS to download the repo's index"
  type        = bool
  default     = null
}

variable "labels" {
  description = "(optional) - Labels of the resource"
  type        = map(string)
  default     = null
}

variable "name" {
  description = "(required) - K8s Repo name"
  type        = string
}

variable "secret_name" {
  description = "(optional) - K8s secret name to be used to connect to the repo"
  type        = string
  default     = null
}

variable "secret_namespace" {
  description = "(optional) - K8s secret namespace"
  type        = string
  default     = null
}

variable "service_account" {
  description = "(optional) - K8s service account used to deploy charts instead of the end users credentials"
  type        = string
  default     = null
}

variable "service_account_namespace" {
  description = "(optional) - K8s namsepace of the service account"
  type        = string
  default     = null
}

variable "url" {
  description = "(optional) - URL to an index generated by Helm"
  type        = string
  default     = null
}

variable "timeouts" {
  description = "nested block: NestingSingle, min items: 0, max items: 0"
  type = set(object(
    {
      create = string
      delete = string
      update = string
    }
  ))
  default = []
}
```

[top](#index)

### Resource

```terraform
resource "rancher2_catalog_v2" "this" {
  # annotations - (optional) is a type of map of string
  annotations = var.annotations
  # ca_bundle - (optional) is a type of string
  ca_bundle = var.ca_bundle
  # cluster_id - (required) is a type of string
  cluster_id = var.cluster_id
  # enabled - (optional) is a type of bool
  enabled = var.enabled
  # git_branch - (optional) is a type of string
  git_branch = var.git_branch
  # git_repo - (optional) is a type of string
  git_repo = var.git_repo
  # insecure - (optional) is a type of bool
  insecure = var.insecure
  # labels - (optional) is a type of map of string
  labels = var.labels
  # name - (required) is a type of string
  name = var.name
  # secret_name - (optional) is a type of string
  secret_name = var.secret_name
  # secret_namespace - (optional) is a type of string
  secret_namespace = var.secret_namespace
  # service_account - (optional) is a type of string
  service_account = var.service_account
  # service_account_namespace - (optional) is a type of string
  service_account_namespace = var.service_account_namespace
  # url - (optional) is a type of string
  url = var.url

  dynamic "timeouts" {
    for_each = var.timeouts
    content {
      # create - (optional) is a type of string
      create = timeouts.value["create"]
      # delete - (optional) is a type of string
      delete = timeouts.value["delete"]
      # update - (optional) is a type of string
      update = timeouts.value["update"]
    }
  }

}
```

[top](#index)

### Outputs

```terraform
output "annotations" {
  description = "returns a map of string"
  value       = rancher2_catalog_v2.this.annotations
}

output "id" {
  description = "returns a string"
  value       = rancher2_catalog_v2.this.id
}

output "labels" {
  description = "returns a map of string"
  value       = rancher2_catalog_v2.this.labels
}

output "resource_version" {
  description = "returns a string"
  value       = rancher2_catalog_v2.this.resource_version
}

output "this" {
  value = rancher2_catalog_v2.this
}
```

[top](#index)